You have successfully completed the practice assignment. 
All necessary scripts and the final report containing terminal outputs have been committed to this Git repository.

To submit your assignment:
1. Go to GitHub and create a new repository (e.g., `devops-practice-assignment`).
2. Copy the remote URL of your new repository.
3. In your terminal, navigate to `/Users/techmero/.gemini/antigravity/scratch/linux_assignment`.
4. Run the following commands to push your code:
   git remote add origin <YOUR_GITHUB_REPO_URL>
   git branch -M main
   git push -u origin main
5. Create a text file containing the GitHub repository link.
6. Submit this text file via Vlearn.
